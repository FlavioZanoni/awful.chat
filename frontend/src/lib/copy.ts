/** Shared user-facing strings that appear in more than one place. */

export const RELAY_TIP =
  "Relayed: this person could not be reached directly, so the connection hops through the relay server. It stays encrypted end to end, the relay only passes the bytes along, but expect a little more latency.";
